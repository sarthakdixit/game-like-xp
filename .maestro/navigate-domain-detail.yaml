# Manual QA carried over from Batch 5: navigate Home -> a domain -> back,
# and confirm the data stays consistent. Run with:
# maestro test .maestro/navigate-domain-detail.yaml
appId: com.chronicle.app
---
- launchApp
- assertVisible: "Chronicle"
- tapOn:
    id: "domain-row-health"
- assertVisible: "Health · level"
- tapOn:
    id: "domain-detail-back"
- assertVisible: "Your standing"
