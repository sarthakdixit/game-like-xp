# Chronicle is now a React web app

This project moved from React Native/Expo to a plain React web app (Vite + TypeScript) backed by
Firebase (Auth, Firestore, Hosting). See `requirements.md`, `constraints.md`, and `PLAN.md` for the
current scope and build plan — there is no Expo/React Native code in this repo anymore.
